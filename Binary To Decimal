#include <bits/stdc++.h>
using namespace std;
int main()
{
//binary to decimal
cout<<"enter a binary no.:"<<endl;
 int b,d,s=0,a=0;
cin>>b;
while(b>0)
{
    int m;
    m=b%10;
    s+= m *(pow(2,a));
    a++;
    b=b/10;
}
cout<<s;
    return 0;
}
